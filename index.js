import React from 'react'
import ReactDOM from 'react-dom';
import App from './App'

ReactDOM.render(
  <App style={style}/>, 
  document.getElementById('root')
);

const style = {
  margin: '0',
  padding: '0'
}
